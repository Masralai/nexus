{
  "name": "nexus",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "nexus": "src/cli/main.ts"
  },
  "scripts": {
    "dev": "bun run src/cli/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:compile": "mkdir -p dist && bun build --compile ./src/cli/main.ts --outfile dist/nexus"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.18",
    "react-devtools-core": "^7.0.1",
    "typescript": "^5"
  },
  "dependencies": {
    "ink": "^7.1.1",
    "react": "^19.2.8"
  }
}